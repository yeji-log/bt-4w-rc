# 블루투스 RC카 웹 컨트롤러 – UI 기능 구현 계획

**목표 기능**
1. 블루투스 기기 **연결 / 연결 해제(취소)**
2. **터미널 탭** – 문자열을 직접 입력해 자유롭게 송수신
3. **게임콘솔(버튼) 탭** – 방향/액션 버튼으로 조작, 각 버튼이 보낼 값을 사용자가 직접 설정 가능
4. **화면 회전 대응** – 태블릿·스마트폰을 가로/세로로 돌리면 레이아웃이 그에 맞게 전환

> 이전 계획서(`블루투스_RC카_웹앱_구현계획.md`)에서 다룬 하드웨어 배선·HM-10 UUID 확인·iOS/Bluefy·HTTPS 제약은 그대로 전제로 깔고, 이 문서는 **웹앱의 화면/기능 구조**에 집중합니다.

---

## 1. 화면 구조 개요

```
┌─────────────────────────────────────┐
│  [연결 상태 표시]   [연결하기 / 연결해제] │  ← 상단 공통 바 (탭과 무관하게 항상 노출)
├─────────────────────────────────────┤
│  [터미널]  [게임패드]                  │  ← 탭 전환
├─────────────────────────────────────┤
│                                       │
│         (선택된 탭 내용 표시)           │
│                                       │
└─────────────────────────────────────┘
```

- **연결 관리는 탭과 분리된 공통 영역**에 둡니다. 터미널 탭에서 연결하고 게임패드 탭으로 넘어가도 연결이 끊기면 안 되기 때문에, BLE 연결 객체는 탭마다 새로 만들지 않고 **앱 전체에서 하나만 공유**하는 구조로 설계합니다.
- 탭은 전송 방식만 다를 뿐, **최종적으로는 같은 BLE characteristic으로 데이터를 씁니다.**

---

## 2. 상태 설계 (무엇을 기억해야 하는가)

| 상태 | 설명 | 저장 위치 |
|---|---|---|
| 연결 상태 | `disconnected` / `connecting` / `connected` | 메모리(전역 변수/상태) |
| BLE device·characteristic 객체 | 연결 후 재사용할 GATT 참조 | 메모리 |
| 터미널 로그 | 송신/수신 기록 리스트 | 메모리 (탭 벗어나도 유지, 새로고침 시 초기화) |
| 게임패드 버튼별 매핑 값 | 버튼 → 전송할 문자열 | **localStorage** (새로고침·재접속해도 유지) |

---

## 3. 단계별 구현 계획

### Phase 0. 화면 설계 정리 (0.5일)
- [ ] 상단 공통 연결 바 와이어프레임 (연결 전/연결 중/연결 후 3가지 상태 UI 스케치)
- [ ] 게임패드 탭 버튼 배치 스케치 (예: 십자 방향키 4개 + 중앙 정지 버튼 + 좌우 액션 버튼 2개 등)
- [ ] 터미널 탭 레이아웃 스케치 (로그 영역 + 하단 입력창 + 전송 버튼)
- [ ] **세로 모드/가로 모드 각각의 레이아웃 와이어프레임** 별도로 스케치 (특히 게임패드 탭은 실제 게임 컨트롤러처럼 가로 모드에서 왼쪽 방향키·오른쪽 액션버튼 배치를 고려)

### Phase 1. BLE 연결/해제 공통 모듈 (1~2일)
- [ ] **연결하기** 버튼 → `navigator.bluetooth.requestDevice()`로 기기 선택 팝업 → GATT 서버 연결 → 서비스/캐릭터리스틱 확보 → notify 구독 시작
- [ ] **연결 해제** 버튼 → `gatt.disconnect()` 호출, 상태를 `disconnected`로 전환
- [ ] 버튼은 상태에 따라 **텍스트와 동작이 토글**되도록 구현 (연결 전엔 "연결하기", 연결 후엔 "연결 해제"로 바뀌는 하나의 버튼, 또는 별도 두 버튼 중 선택)
- [ ] 기기 쪽에서 갑자기 연결이 끊겼을 때(`gattserverdisconnected` 이벤트)를 감지해 UI 상태를 자동으로 `disconnected`로 되돌리는 로직 필수 (수동으로 끊은 게 아닌데 버튼이 "연결됨"으로 남아있는 버그 방지)
- [ ] 이 모듈은 터미널 탭·게임패드 탭 어디서든 import해서 쓰는 **공용 모듈**로 분리 설계 (예: `ble.js`)

### Phase 2. 터미널 탭 구현 (1~2일)
- [ ] 하단에 텍스트 입력창 + 전송 버튼(Enter 키 전송도 지원)
- [ ] 전송한 문자열은 로그 영역에 "보냄" 표시로 기록
- [ ] HM-10에서 notify로 들어오는 데이터를 실시간으로 로그에 "받음" 표시로 기록 (송신/수신 구분: 색상 또는 좌우 정렬로 구분)
- [ ] 문자열 ↔ 바이트 변환 처리 (`TextEncoder`/`TextDecoder`), 개행문자(`\n`, `\r\n`) 포함 여부를 옵션으로 둘지 결정
- [ ] 로그 자동 스크롤, "로그 지우기" 버튼
- [ ] 연결 안 된 상태에서는 입력창/전송 버튼 비활성화 처리

### Phase 3. 게임패드 탭 – 기본 레이아웃 (1~2일)
- [ ] 방향 버튼(상/하/좌/우) + 정지 버튼 + 필요시 액션 버튼(부스터, 경적 등)을 게임콘솔 느낌으로 배치
- [ ] 버튼 입력 방식 결정: **누르는 동안 계속 전송**(mousedown/touchstart~mouseup/touchend, 예: 이동 중 계속 F 전송) vs **클릭 1회 전송**(단발성 명령) — 이동 버튼은 전자, 경적 같은 액션은 후자가 자연스러움
- [ ] 모바일 터치 이벤트 대응: `touchstart`/`touchend` 사용, 버튼 영역에서 스크롤·확대 등 브라우저 기본 동작 방지(`touch-action: none` 등)
- [ ] 버튼 크기를 충분히 크게(스마트폰 터치 기준) 잡아 오조작 방지
- [ ] 세로/가로 모드에서 버튼 배치가 달라질 것을 감안해, 버튼 렌더링 로직을 방향 값(portrait/landscape)에 따라 다른 배치를 적용할 수 있도록 구조화 (Phase 5에서 실제 전환 로직 연결)

### Phase 4. 게임패드 탭 – 버튼 값 커스터마이징 (1~2일)
- [ ] "설정 모드" 진입 방법 결정 (예: 상단에 "버튼 설정" 토글, 또는 버튼 길게 누르기로 편집 팝업 오픈)
- [ ] 설정 모드에서 버튼 선택 시, 해당 버튼이 전송할 **문자열 값을 입력하는 모달/인풋** 표시
- [ ] 입력한 매핑 값을 **localStorage에 저장** → 새로고침하거나 나중에 다시 접속해도 설정 유지
- [ ] "기본값으로 초기화" 버튼 제공 (예: 상=`F`, 하=`B`, 좌=`L`, 우=`R`, 정지=`S`로 되돌리기) — 학생들이 잘못 설정해서 조작이 안 될 때 복구 수단 필요
- [ ] 설정 모드와 실제 조작 모드를 명확히 구분되는 UI로 표시 (예: 설정 모드일 땐 버튼 테두리 색이 바뀌는 등) — 조작 중 실수로 값이 바뀌는 것 방지

### Phase 5. 화면 회전(가로/세로) 대응 구현 (1일)
- [ ] `<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">` 로 뷰포트 기본 설정
- [ ] CSS `@media (orientation: portrait)` / `@media (orientation: landscape)` 로 레이아웃 두 벌 정의 (JS 없이 CSS만으로 우선 처리)
- [ ] 게임패드 탭: **가로 모드**에서는 실제 게임 컨트롤러처럼 왼쪽에 방향키, 오른쪽에 액션 버튼을 배치(양손 엄지 조작 고려), **세로 모드**에서는 버튼을 화면 하단에 컴팩트하게 배치하거나 "가로로 돌리면 더 편하게 조작할 수 있어요" 안내 문구 표시
- [ ] 터미널 탭: 세로/가로 모두 큰 변경 없이 로그 영역 높이만 방향에 맞게 조정 (텍스트 입력·확인은 세로 모드가 오히려 편함)
- [ ] 방향을 강제로 고정(lock)하지 않고 **자동으로 따라가도록** 구현 — Screen Orientation API의 `lock()`은 브라우저 지원이 제한적이고 대부분 전체화면 상태에서만 동작하므로, 회전 감지는 CSS 미디어쿼리 + 필요한 경우 JS `matchMedia('(orientation: landscape)')`/`orientationchange` 이벤트로 처리
- [ ] iOS Safari/Bluefy에서 주소창 표시 여부에 따라 `100vh` 계산이 흔들리는 문제 대비 → `100dvh` 단위 사용 또는 JS로 실제 뷰포트 높이 재계산
- [ ] **회전 시 BLE 연결이 끊기지 않는지 실기기로 직접 확인** (페이지 리로드가 아니라 레이아웃만 바뀌는 것이므로 이론상 연결이 유지되지만, 기기·브라우저별로 다를 수 있어 반드시 실측 필요 — 특히 iOS Bluefy)

### Phase 6. 통합 및 상태 동기화 (1일)
- [ ] 연결이 안 된 상태에서는 터미널 전송 버튼과 게임패드 버튼 모두 비활성화(회색 처리)되는지 확인
- [ ] 탭을 전환해도, 화면을 회전해도 BLE 연결 상태가 유지되는지 확인 (연결 객체가 탭/방향별로 중복 생성되지 않는지 점검)
- [ ] 터미널에서 보낸 값과 게임패드에서 보낸 값이 실제로 같은 characteristic을 통해 동일하게 아두이노에 도달하는지 교차 테스트

### Phase 7. UX 다듬기 (선택, 0.5~1일)
- [ ] 연결 끊김 시 토스트/배너로 알림 표시
- [ ] 게임패드 탭에서 버튼을 누를 때 시각적 피드백(눌림 효과) 추가
- [ ] 회전 전환 시 레이아웃이 자연스럽게 바뀌는지(깜빡임·버튼 밀림 없는지) 다듬기

### Phase 8. 배포 및 테스트 (0.5일)
- [ ] GitHub Pages에 배포 (기존 계획서의 HTTPS 요건 충족)
- [ ] 안드로이드 Chrome / 아이폰 Bluefy 양쪽에서 두 탭 모두, 세로·가로 모드 모두 교차 테스트
- [ ] 실제 RC카에 연결해 터미널 탭·게임패드 탭 각각으로 전/후/좌/우/정지 명령이 정상 동작하는지, 회전 후에도 정상 동작하는지 최종 검증

---

## 4. 파일 구조 제안 (참고용)

```
/index.html          → 탭 UI, 상단 연결 바
/ble.js               → BLE 연결/해제/전송/수신 공용 모듈 (핵심: 싱글톤으로 하나만 유지)
/terminal.js           → 터미널 탭 로직
/gamepad.js            → 게임패드 탭 로직 (버튼 렌더링 + 입력 이벤트)
/storage.js             → 버튼 매핑 값 localStorage 저장/불러오기
/style.css              → 공통 스타일
```

---

## 5. 주의사항 요약

- **BLE 연결 객체는 반드시 하나만 유지**하세요. 터미널 탭과 게임패드 탭이 각자 연결을 새로 만들면 연결 충돌이나 중복 연결 문제가 생깁니다.
- 게임패드 버튼을 "누르는 동안 계속 전송" 방식으로 만들 경우, 너무 잦은 `writeValue()` 호출은 BLE 대역폭·아두이노 처리 속도를 넘어설 수 있으니 **일정 간격으로 제한(쓰로틀링)**하는 걸 권장합니다.
- 버튼 매핑 값에 개행문자를 포함할지 여부는 아두이노 펌웨어의 명령 파싱 방식과 반드시 맞춰야 합니다 (예: 아두이노가 `\n` 기준으로 명령을 구분한다면 터미널·게임패드 양쪽 모두 동일하게 개행을 붙여 전송).
- iOS(Bluefy)·HTTPS 관련 제약은 이전 계획서 내용과 동일하게 적용됩니다.
- 화면 회전은 **CSS 미디어쿼리 기반 반응형 처리**를 기본으로 하고, 강제 방향 고정(Screen Orientation lock)은 지원이 불안정하니 지양하는 것을 권장합니다. 회전 후 BLE 연결 유지 여부는 반드시 실기기 테스트로 확인하세요.
