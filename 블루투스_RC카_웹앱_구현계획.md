# 블루투스 4륜구동 RC카 – 웹 기반 컨트롤러 구현 계획

**대상 하드웨어:** Arduino Uno, HM-10(BLE 모듈), L298N 모터드라이버 x1, DC 모터 x4
**목표:** 전용 앱 대신 웹페이지(스마트폰 브라우저 포함)로 RC카를 조작

---

## 1. 결론: 실행 가능한가?

**가능합니다.** 이유는 다음과 같습니다.

- HM-10은 **BLE(Bluetooth Low Energy)** 모듈입니다. 웹 표준 중 **Web Bluetooth API**가 정확히 BLE(GATT) 기기를 대상으로 하기 때문에, 별도의 앱 없이 **Chrome 계열 브라우저에서 직접 HM-10에 연결**할 수 있습니다.
- 웹으로 만들면 앱스토어 배포 없이 **URL(또는 QR코드) 하나**로 학생들에게 배포할 수 있어, 여러 명이 각자 스마트폰으로 테스트하기에 유리합니다.

### 다만 반드시 알아야 할 제약 2가지

| 제약 | 내용 |
|---|---|
| **iOS(아이폰) 미지원** | Apple은 Safari에서 Web Bluetooth API를 지원하지 않습니다. 아이폰 학생은 일반 브라우저로는 접속해도 블루투스 연결 버튼이 동작하지 않습니다. (우회: **Bluefy**라는 서드파티 iOS 브라우저 앱을 쓰면 가능하지만, 별도 앱 설치가 필요해서 "웹으로 하면 앱 설치가 필요 없다"는 장점이 아이폰에서는 사라집니다.) |
| **HTTPS 필수** | Web Bluetooth는 보안 컨텍스트(HTTPS 또는 localhost)에서만 동작합니다. `http://`로 열면 아예 API가 비활성화됩니다. → GitHub Pages 등 무료 HTTPS 호스팅 사용을 권장합니다. |

> 💡 **참고**: 기존에 전용 앱으로 연결이 잘 안 됐던 이유도 상당 부분 여기서 짐작할 수 있습니다. HM-10은 BLE라서 스마트폰의 "일반 설정 > 블루투스" 목록에서 페어링하는 방식(클래식 블루투스, SPP)과는 접속 절차 자체가 다릅니다. 아이폰이라면 시스템 설정에 아예 안 보이는 게 정상이고, 안드로이드 앱들도 Classic/BLE 프로토콜을 혼동하는 경우가 흔합니다. 이 부분은 웹으로 가더라도 원리는 같으니, 시작 전에 **팀 내 학생들이 안드로이드폰인지 아이폰인지부터 파악**해두시는 걸 추천합니다.

---

## 2. 전체 시스템 구조

```
[스마트폰/PC 웹브라우저]
     │  Web Bluetooth API (BLE GATT)
     ▼
[HM-10 모듈]
     │  UART (SoftwareSerial, 보통 9600bps)
     ▼
[Arduino Uno]
     │  디지털/PWM 출력
     ▼
[L298N 모터드라이버] ──▶ DC 모터 x4 (좌측 2개 병렬 / 우측 2개 병렬)
```

- L298N은 채널이 2개(A/B)뿐이므로, 모터 4개는 보통 **좌측 앞뒤 2개를 OUT1/2에 병렬, 우측 앞뒤 2개를 OUT3/4에 병렬**로 연결해 좌우 2채널로 제어합니다(탱크 조향 방식).

---

## 3. 사전 확인 사항 (개발 착수 전 필수)

1. **HM-10 서비스/캐릭터리스틱 UUID 확인**
   - 정품/클론에 따라 다를 수 있으나 보통 Service `0xFFE0`, Characteristic `0xFFE1`.
   - 스마트폰에 **nRF Connect for Mobile** 앱을 깔고 HM-10을 스캔해 실제 UUID를 직접 확인.
2. **HM-10 AT 명령 응답 확인** — 펌웨어 버전, 기본 baud rate(보통 9600) 점검.
3. **학생들 보유 기기 파악** — 안드로이드/아이폰 비율, PC 사용 가능 여부.
4. **HTTPS 호스팅 준비** — GitHub Pages 계정/저장소 준비(무료, 자동 HTTPS).

---

## 4. 단계별 구현 계획

### Phase 0. 환경 진단 (0.5일)
- [ ] nRF Connect로 HM-10 UUID·기기명 확인
- [ ] 팀 내 스마트폰 OS 조사 (아이폰 있으면 Bluefy 설치 또는 안드로이드 기기로 테스트 담당 지정)
- [ ] GitHub 계정/저장소 생성 (또는 학교 내 다른 정적 호스팅)

### Phase 1. 하드웨어 배선 점검 (1일)
- [ ] L298N ENA/ENB → 아두이노 PWM 핀 연결 (속도 제어용)
- [ ] 좌/우 모터 페어링 배선 확인 (좌측 2개 병렬, 우측 2개 병렬)
- [ ] HM-10 TX/RX ↔ 아두이노 SoftwareSerial 핀 연결
  - ⚠️ HM-10 RX는 3.3V 로직입니다. 아두이노 TX(5V)를 직결하면 장기적으로 모듈이 손상되거나 통신이 불안정할 수 있어 **전압 분배 저항(또는 레벨시프터)** 권장
- [ ] **모터 전원과 HM-10/아두이노 로직 전원을 분리**
  - 모터 구동 시 발생하는 전류 스파이크·노이즈가 HM-10을 리셋시키거나 BLE 연결을 끊는 원인이 되는 경우가 많습니다. 기존 연결 불안정 문제의 숨은 원인일 가능성이 있으니 우선순위 높게 점검 권장.

### Phase 2. 아두이노 펌웨어 – 명령 파싱 & 모터 제어 (1~2일)
- [ ] 간단한 시리얼 프로토콜 설계 (예: 단일 문자 `F`/`B`/`L`/`R`/`S` + 필요시 속도값)
- [ ] SoftwareSerial로 HM-10 수신 데이터를 읽어 L298N 제어 함수에 매핑하는 로직 작성
- [ ] **웹 연동 전에** 아두이노 시리얼 모니터(USB 유선)로 먼저 모터 동작 검증 → 문제 발생 시 "펌웨어 문제"와 "블루투스 문제"를 분리해서 진단 가능

### Phase 3. HM-10 단독 블루투스 통신 테스트 (0.5~1일)
- [ ] nRF Connect 또는 Serial Bluetooth Terminal 같은 BLE 시리얼 앱으로 HM-10에 연결해 명령 송수신이 정상 동작하는지 먼저 검증
- [ ] 이 단계를 건너뛰고 바로 웹 개발로 가면, 문제가 생겼을 때 "하드웨어/HM-10 문제"인지 "웹 코드 문제"인지 구분하기 어려워짐 → 반드시 분리 검증 권장

### Phase 4. 웹앱 – BLE 연결 기본 골격 (1~2일)
- [ ] `navigator.bluetooth.requestDevice()`로 기기 스캔 (기기명 또는 서비스 UUID로 필터링)
- [ ] GATT 서버 연결 → 서비스/캐릭터리스틱 획득
- [ ] 연결 상태 표시 UI, 연결 끊김 감지 및 재연결 로직

### Phase 5. 웹앱 – 조작 UI 구현 (1~2일)
- [ ] 방향 버튼(전/후/좌/우/정지) 또는 가상 조이스틱 UI
- [ ] 버튼 입력 → `writeValue()`로 명령 전송 (과도한 연속 전송 방지용 쓰로틀링 적용)
- [ ] 속도 조절 슬라이더 (필요시 PWM 값을 명령에 포함)

### Phase 6. 통합 테스트 및 튜닝 (1~2일)
- [ ] 명령 전송 ~ 모터 반응까지의 지연시간 측정
- [ ] 명령 유실 대비 재전송/디바운스 로직
- [ ] 배터리·모터 노이즈로 인한 연결 끊김 재현 테스트 (Phase 1의 전원 분리가 효과 있는지 확인)
- [ ] 여러 학생이 동시에 각자 기기로 접속할 때 상호 간섭 여부 확인

### Phase 7. 배포 및 운영 (0.5일)
- [ ] GitHub Pages로 웹앱 배포 (HTTPS 자동 제공)
- [ ] 학생 배포용 QR코드 생성
- [ ] **아이폰 사용 학생 대응 방침 확정**: Bluefy 앱 안내 또는 안드로이드 기기 사용 조 편성
- [ ] 사용 매뉴얼 작성 (최초 연결 방법, 자주 발생하는 오류와 체크리스트)

---

## 5. 리스크 및 대안

| 리스크 | 대응 방안 |
|---|---|
| 아이폰 Web Bluetooth 미지원 | Bluefy 브라우저 앱 사용, 또는 안드로이드/PC로 조 편성. 장기적으로는 **ESP32 + WiFi AP + WebSocket** 방식으로 전환하면 iOS 포함 완전 크로스플랫폼 지원 가능(단, 이번 학기 하드웨어 교체가 필요해 별도 검토 필요) |
| HM-10 클론 제품 UUID 상이 | 사전에 nRF Connect로 반드시 실측 확인 |
| 모터 구동 노이즈로 BLE 연결 끊김 | 전원 계통 분리, 디커플링 커패시터 추가 |
| 다수 학생 동시 스캔 시 기기 선택 혼동 | `AT+NAME` 명령으로 HM-10 모듈마다 고유한 이름 부여 |

---

## 6. 참고 자료
- Web Bluetooth API 문서 (MDN Web Docs, developer.chrome.com)
- HM-10 AT 명령어 레퍼런스
- nRF Connect for Mobile (기기 스캔/UUID 확인용)
- Bluefy – iOS용 Web Bluetooth 지원 브라우저
